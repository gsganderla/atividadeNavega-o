import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    section: {
        width: '80%',
        justifyContent: "flex-start"
    },
    title: {
        fontSize: 18,
        fontWeight: "bold",
        color: '#333333'
    }
  });